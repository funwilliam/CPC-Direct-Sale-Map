# 複製為 .env.local 並填入實際值（.env.local 不進 git）
# Google Maps JS API key（GCP 需設 HTTP referrer 限制 + 配額上限 + 預算警示，見 ADR-002）
VITE_GOOGLE_MAPS_API_KEY=your-key-here
# 正式向量 Map ID（GCP Maps Platform > 地圖管理 建立，JavaScript+向量）；未設走 DEMO_MAP_ID
VITE_GOOGLE_MAPS_MAP_ID=
